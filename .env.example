# Copy to .env.local for local overrides (optional).
# Local API:
VITE_API_URL=http://localhost:3000

# Production API (set in Vercel → Environment Variables):
# VITE_API_URL=https://team-flow-be.vercel.app
